BOY & MAN
=========

How to install the project
--------------------------

* `npm install`


How to build the project
------------------------

* `npm run build` will run webpack once.

* `npm run builddev` will run webpack in dev mode (`--watch` option).
